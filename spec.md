# 名簿管理アプリ　仕様書
## 【概要】
本アプリケーションは、CLI（Command Line Iterface）を持つ、社員名簿管理アプリケーションである。
1. 起動されるとメインメニューを表示し、コマンドを受け付ける状態になっている。
1. 社員名簿に社員を登録・更新・削除することが出来る。
1. 社員名簿を一覧表示することが出来る。
1. 社員名簿をCSV形式でファイルに保存することが出来る。
1. CSVファイルを指定して社員名簿を更新することが出来る。
1. 社員名簿を検索することが出来る。
### 目的
本アプリケーションは、小規模な組織における社員情報の一元管理と事務作業の効率化を目的とする。
* データの永続化：入力した情報をCSV形式で保存・読み込み可能にすることで、再起動後も継続して利用できる。
* マルチプラットフォーム対応：Windows環境だけでなくLinux環境でも動作し、幅広いPC環境での名簿管理をサポート。
### 開発環境
* 使用ツール：サクラエディタ
* 言語：C言語（コンパイラの都合上.cppとして作成）
* コンパイラ：bcc32（Borland C++）/gcc（Linuxでの移植確認用）
* 文字コード：Shift-JIS (bcc32環境) / UTF-8 (Linux環境)
## 【機能一覧】
1. 登録：名簿情報の入力、社員コード昇順で登録
    * 入力された社員コードが既に登録されている場合、メインメニューに戻る
    * 社員コード、氏名、年齢、性別、電話番号の順に入力
    * 社員コードは5桁の数字で、5桁に満たない場合は左側が0埋めされる
    * 氏名は半角英数字で、30文字まで入力可（空白も入力可）
    * 年齢は0～127の数字
    * 性別は１が男性、０が女性で必須選択
    * 電話番号は15桁の数字（空白も入力可）
2. 変更：名簿に登録されている各項目について変更
    * 社員コード指定すると完全一致検索（0埋め済みのもの）で該当名簿を表示
    * 項目を選択し変更できる
    * 社員コードを変更した場合も、登録されているリストは『社員コード昇順』が保たれる（重複チェック、リストのつなぎ直し）
3. 削除：登録されている名簿を削除
    * 1件削除、全件削除が可能
    * 1件削除では、社員コード指定すると完全一致検索（0埋め済みのもの）で該当名簿を表示
    * 削除前に確認メッセージ表示（Y/N選択）
4. 一覧表示：登録されている名簿の一覧表示
    * 社員コード昇順、もしくは名簿をソートして表示する
    * ソート：社員コード降順、氏名昇順、氏名降順、年齢昇順、年齢降順
    * 10件ごとに表示し、キー入力で次の10件も表示される
5. 保存：登録されている名簿情報をCSV形式で保存
    * 一覧表示機能で見られる名簿（現在登録されている名簿）について、名前を付けて保存する
    * CSV形式（1件1行）：社員コード,氏名,年齢・性別(bit),電話番号
    * 保存される場所はアプリの実行ファイルと同じフォルダ内
    * 拡張子（.txt）まで入力するよう誘導
    * 既存のファイル名が入力された場合、上書き保存もしくは処理中止の選択
6. 読み込み：CSV形式で保存した名簿情報をアプリ内へ持ってくる
    * アプリの実行ファイルと同じフォルダに存在する.txtのファイルを、読み込み可能ファイルとして一覧表示（最大20件）
    * ファイル名に番号が振られ、読み込みたいファイルの番号入力で選択
    * 以下の不正データの読み込みスキップ
        * 社員コードが重複しているデータ
        * いずれかの項目について、既定の文字数超過しているデータ
        * 年齢が128以上のデータ
7. 検索：既存名簿の検索機能
    * 社員コードまたは氏名での検索
    * あいまい検索（部分一致、大文字小文字の区別なし）
## 【データ構造】
構造体（struct meibot）を単方向連結リストで管理
| 項目 | 型 | 説明 |
| :--- | :--- | :--- |
| 社員コード | char[6] | 5桁、一意、0埋め |
| 氏名 | char[31] | 30文字まで入力可 |
| 性別・年齢 | unsigned char | 性別は0または1、年齢は0～127歳を2進数変換、合わせて8ビットで管理 |
| 電話番号 | char[16] | 15文字まで入力可 |
## 【操作方法】
* キーボード入力
    * メニュー選択：選択画面にて、表示されている各機能に対応した数字（【機能一覧】参照）を入力すると該当の処理をする
    * メインメニューで９を入力するとアプリが終了する
    * 文字列入力：半角英数字、空白、記号
    * カーソル移動：左右矢印キー
* 内部処理として`文字列入力用サブルーチン`を使用
    * 英数字（と空白）入力、数字（と空白）入力、なんでも（英数字と記号）入力、0/1入力、Y/N（大文字、小文字どちらも）入力に対応
    * Enterキー、Backspaceキー、Insertキーに対応
    * 左右のカーソル移動に対応
## 【画面遷移】
* メインメニュー：１～７の機能（機能一覧参照）と９（アプリ終了）
1. 登録
    1. 社員コード、氏名、年齢、性別、電話番号の順に入力
    2. 入力された項目を表示し、登録するかの確認（Y/N選択）の後、メインメニューに戻る
1. 変更
    1. 変更したい名簿の社員コードを入力
    2. 該当する名簿があれば表示、なければメインメニューに戻る
    3. 変更したい項目を選択（1:社員コード 2:氏名 3:年齢 4:性別 5:電話番号 9:終了）
    4. ９入力でメインメニューに戻る
1. 削除
    1. 1件削除または全件削除のメニュー表示    
    2. (1件削除選択時)該当する名簿があれば表示、なければメインメニューに戻る
    3. 削除前に確認メッセージ表示（Y/N選択）
    4. 削除（Y）または中止（N）後、メインメニューに戻る
1. 一覧表示
    1. 表示モードの選択メニュー表示（1：社員コード昇順 2:社員コード降順 3:氏名昇順 4:氏名降順 5:年齢昇順 6:年齢降順 9:メインメニューに戻る）
    2. 10件ずつ表示、それ以降はキー入力で表示
    3. 全件表示後、キー入力でメインメニューに戻る    
1. 保存
    1. ファイル名の入力（.txt入力誘導）
    2. 既存のファイル名が入力された場合、上書き保存（Y）もしくは処理中止（N）の選択
    3. 保存もしくは中止後、メインメニューに戻る
1. 読み込み
    1. 読み込み可能なファイル一覧を表示
    2. 指定した番号のファイルを読み込み
    3. 不正データはスキップし、読み込み件数を表示後メインメニューに戻る
1. 検索
    1. 社員コード検索または氏名検索のメニュー表示
    2. 検索文字列の入力
    3. 該当名簿の表示
    4. メインメニューに戻る
* メインメニューに戻る際に画面がリセットされる